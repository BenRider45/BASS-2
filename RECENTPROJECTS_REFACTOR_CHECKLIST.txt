RecentProjects → QAbstractListModel: Required Changes
=====================================================================

NEW FILES TO CREATE
=====================================================================

src/recentprojectslistmodel.h
-----------------------------
- Class inheriting QAbstractListModel with Q_OBJECT
- Roles enum starting at Qt::UserRole + 1:
  * ProjectNameRole
  * ProjectPathRole
  * LastAccessedRole
  * ProjectIdRole
  * IsCurrentRole
- Q_PROPERTY(int count READ count NOTIFY countChanged)
- Private members:
  * QVector<RecentProject> m_projects
  * QUuid m_currentProjectId
- Override declarations: rowCount, data, roleNames
- Public methods:
  * void setProjects(const QVector<RecentProject> &) - bulk load on startup
  * void onProjectOpened(const RecentProject &) - move to top, update timestamp, set current
  * void setCurrentProjectId(const QUuid &) - updates isCurrent role for relevant rows
  * QVector<RecentProject> projects() const - used by ProjectManager for persistence


src/recentprojectslistmodel.cpp
-------------------------------
- rowCount: guard parent.isValid(), return m_projects.size()
- data: switch on role, return appropriate field from RecentProject
- roleNames: return hash mapping roles to:
  * ProjectNameRole → "projectName"
  * ProjectPathRole → "projectPath"
  * LastAccessedRole → "lastAccessed"
  * ProjectIdRole → "projectId"
  * IsCurrentRole → "isCurrent"
- onProjectOpened:
  1. Find existing entry by UUID
  2. beginRemoveRows/endRemoveRows if found
  3. Set _lastAccessed = QDateTime::currentDateTime()
  4. beginInsertRows(0,0)/endInsertRows to prepend
  5. Call setCurrentProjectId()
- setCurrentProjectId: emit dataChanged for old and new current rows with {IsCurrentRole}


FILES TO MODIFY
=====================================================================

CMakeLists.txt
---------------
- Add src/recentprojectslistmodel.cpp to SOURCES list
- Add src/recentprojectslistmodel.h to HEADERS list


src/projectmanager.h
--------------------
- Add #include "recentprojectslistmodel.h"
- Replace Q_PROPERTY recentProjects:
  OLD: Q_PROPERTY(QVariantList recentProjects READ recentProjects NOTIFY recentProjectsChanged)
  NEW: Q_PROPERTY(RecentProjectsListModel* recentProjects READ recentProjects CONSTANT)
- Replace getter declaration:
  OLD: QVariantList recentProjects() const;
  NEW: RecentProjectsListModel* recentProjects() const;
- Replace private member:
  OLD: QVariantList m_recentProjects;
  NEW: RecentProjectsListModel* m_recentProjectsModel;
- Remove signal: void recentProjectsChanged();


src/projectmanager.cpp
----------------------
- Constructor: allocate m_recentProjectsModel = new RecentProjectsListModel(this)
- Startup initialization (~line 62):
  * After extractRecentProjectList(), convert QVariantList result to QVector<RecentProject>
  * Call m_recentProjectsModel->setProjects(...)
  * Remove emit recentProjectsChanged()
- recentProjects() getter: return m_recentProjectsModel
- updateRecentProjects():
  * Replace entire manual find/replace/append loop with:
    m_recentProjectsModel->onProjectOpened(projData)
  * Manage RecentProjectsDirty flag accordingly
- updateRecentProjectsFile() (~line 97):
  * Replace: for (auto project : m_recentProjects)
  * With: for (const auto &proj : m_recentProjectsModel->projects())
- loadRecentProject() (~line 190):
  * Replace: for (auto val : m_recentProjects)
  * With: iteration over m_recentProjectsModel->projects()


qml/dialogs/ProjectSelectWindow.qml
-----------------------------------
- Update ListView delegate role access - replace all modelData[SharedConstants.X][SharedConstants.Y]:
  OLD: modelData[SharedConstants.PROJECT_DATA][SharedConstants.PROJECT_NAME]
  NEW: projectName

  OLD: modelData[SharedConstants.PROJECT_DATA][SharedConstants.PROJECT_DIR]
  NEW: projectPath

  OLD: modelData[SharedConstants.PROJECT_DATA][SharedConstants.PROJECT_LAST_ACCESSED]
  NEW: lastAccessed

  OLD: modelData[SharedConstants.PROJECT_ID]
  NEW: projectId
- Update openingRecentProject(...) call to pass projectId role
- Add visual feedback for isCurrent (e.g., different border.color or background color when isCurrent === true)
- Remove dead ListModel block at bottom of file (lines 164-192) - never used


KEY NOTES
=====================================================================

✓ QML binding stays the same: model: projectManager.recentProjects still works
  (now returns model pointer instead of QVariantList, which ListView handles natively)

✓ Q_DECLARE_METATYPE(RecentProject) in recentProject.h can stay
  (still used by MetaManager serialization)

✓ Ownership: passing this (ProjectManager) as parent to RecentProjectsListModel
  means Qt handles cleanup automatically

✓ No changes needed to:
  - MetaManager
  - serializationtraits
  - Test files
